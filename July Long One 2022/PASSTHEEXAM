#include <bits/stdc++.h>
#define ll long long

using namespace std;
void solution(){
    int a,b,c;
    cin>>a>>b>>c;
    if(a<10 || b<10 || c<10)cout<<"FAIL"<<endl;
    else if(a+b+c<100)cout<<"FAIL"<<endl;
    else cout<<"PASS"<<endl;
    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int t;
    cin>>t;
    while(t--){
        solution();
    }

    return 0;
}
