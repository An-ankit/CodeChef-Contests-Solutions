#include <bits/stdc++.h>
#define ll long long

using namespace std;
void solution(){
    int x,y;
    cin>>x>>y;
    cout<<max(x,y)<<endl;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int t;
    cin>>t;
    while(t--){
        solution();
    }

    return 0;
}
